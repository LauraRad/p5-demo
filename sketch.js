var bouncables = [];
var frequency = 0

var setupScript = function() {};
var drawScript = function() {};

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(255);

  drawScript();
}
